# Mock Engine

The mock engine is deterministic and intentionally shallow.

It exists to demonstrate the public artifact flow:

1. choose a toy surface kind
2. choose toy questions
3. produce toy assumptions
4. produce a synthetic Build Contract
5. format it as markdown
6. compare a builder report shape against required fields

It must not import production Brieftify logic.

